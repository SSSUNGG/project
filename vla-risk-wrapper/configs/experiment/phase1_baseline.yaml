name: phase1_baseline

tasks:
  - peg_insertion
  - assembling_kits
  - pick_single_ycb
  - pick_cube
  - stack_cube

eval:
  n_seeds: 5
  n_episodes_per_seed: 100
  bootstrap_n: 1000
  confidence_level: 0.95

logging:
  per_episode: true
